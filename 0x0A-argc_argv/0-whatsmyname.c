#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - print the of program
 * @argc: argument count
 * @argv: arguments
 * Return: Always 0 (Success)
 */

int main(int __attribute__((unused)) rgc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
