#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - print the of program
 * @argc: argument count
 * @argv: arguments
 * Return: Always 0 (Success)
 */

int main(int argc, char __attribute__((unused)) *argv[])
{
	printf("%d\n", argc);
	return (0);
}
