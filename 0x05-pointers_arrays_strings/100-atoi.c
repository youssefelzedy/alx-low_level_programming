#include "main.h"
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

/**
 *_atoi -  print n number in array
 *@s: is a string
 *Return: the convert number
 */

int _atoi(char *s)
{
	int i;

	i = atoi(s);
	return (i);
}
